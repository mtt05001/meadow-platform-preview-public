// Removed from the admin portal — will be re-added to the future
// facilitator/client portals. Restore from git history when ready.
import { notFound } from "next/navigation";

export default function SchedulePage() {
  notFound();
}
